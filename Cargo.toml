[package]
name = "navabilitysdk"
version = "0.1.0-rc10"
authors = ["NavAbility™ by WhereWhen.ai Technologies Inc."]
edition = "2021"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu", "wasm32-unknown-unknown"]

[lib]
crate-type = ["rlib"]

[dependencies]
serde = "^1.0"
serde_json = "^1.0"
base64 = "^0.22"
chrono = "^0.4"
log = "^0.4"
regex = "^1.8"
uuid = { version ="^1.15", features = ["v4","v5"] }
url = "^2"
wasm-bindgen-futures = { version = "^0.4", optional = true }
graphql_client = "^0.14"
reqwest = {version = "^0.12", optional=true, features = [
  "json",
  # "multipart",
] }
reqwest-eventsource = "0.6"
futures = "^0.3"
# for blocking calls, but features deps not 100% sorted out yet TODO
# graphql_client = { version="0.14", features = ["reqwest","reqwest-blocking"] }


[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tokio = { version = "^1.40", optional=true, default-features = false, features = ["rt-multi-thread",] }
tracing = "^0.1"


[target.'cfg(target_arch = "wasm32")'.dependencies]
gloo-console = { version = "^0.3", optional = true }

# TBD, likely leads to incorrect Time.rs dependency issue on wasm targets
# [build-dependencies]
# graphql_client_cli = "^0.14"

[features]
wasm = ["dep:reqwest", "dep:wasm-bindgen-futures"]
wasm-dev = ["dep:gloo-console"]
tokio = ["dep:tokio", "dep:reqwest"]
own_runtime = []
thread = ["dep:reqwest"]
blocking = ["graphql_client/reqwest","graphql_client/reqwest-blocking"]

# Optimize all dependencies even in debug builds:
[profile.dev.package."*"]
opt-level = 2

[profile.release]
opt-level = 'z' # fast and small wasm
lto = true
